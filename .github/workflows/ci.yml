name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:

  # ── Job 1: Tests de auth-service ─────────────────────────────────
  test-auth-service:
    name: 🧪 Test Auth Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4
        # Descarga tu código en la VM de GitHub

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: auth-service/package-lock.json
          # cache: reutiliza node_modules si package.json no cambió
          # ahorra 30-60 segundos en cada run

      - name: Instalar dependencias
        working-directory: auth-service
        run: npm ci
        # npm ci es más estricto que npm install
        # usa exactamente las versiones de package-lock.json

      - name: Correr tests
        working-directory: auth-service
        run: npm test

  # ── Job 2: Tests de tasks-service ────────────────────────────────
  test-tasks-service:
    name: 🧪 Test Tasks Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restaurar dependencias
        working-directory: tasks-service
        run: dotnet restore

      - name: Build
        working-directory: tasks-service
        run: dotnet build --no-restore

      - name: Correr tests
        working-directory: tasks-service
        run: dotnet test --no-build

  # ── Job 3: Build y push de imágenes Docker ────────────────────────
  build-and-push:
    name: 🐳 Build & Push Docker Images
    runs-on: ubuntu-latest
    needs: [test-auth-service, test-tasks-service]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    env:
      REGISTRY: ghcr.io
      IMAGE_PREFIX: ghcr.io/${{ github.repository }}

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Login a GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build y push auth-service
        uses: docker/build-push-action@v5
        with:
          context: ./auth-service
          push: true
          tags: |
            ghcr.io/${{ github.repository }}-auth:latest
            ghcr.io/${{ github.repository }}-auth:${{ github.sha }}

      - name: Build y push tasks-service
        uses: docker/build-push-action@v5
        with:
          context: ./tasks-service
          push: true
          tags: |
            ghcr.io/${{ github.repository }}-tasks:latest
            ghcr.io/${{ github.repository }}-tasks:${{ github.sha }}

      - name: Build y push notifications-service
        uses: docker/build-push-action@v5
        with:
          context: ./notifications-service
          push: true
          tags: |
            ghcr.io/${{ github.repository }}-notifications:latest
            ghcr.io/${{ github.repository }}-notifications:${{ github.sha }}

      - name: Build y push api-gateway
        uses: docker/build-push-action@v5
        with:
          context: ./api-gateway
          push: true
          tags: |
            ghcr.io/${{ github.repository }}-gateway:latest
            ghcr.io/${{ github.repository }}-gateway:${{ github.sha }}

      - name: Resumen del pipeline
        run: |
          echo "## 🚀 Deploy exitoso" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Imágenes publicadas:**" >> $GITHUB_STEP_SUMMARY
          echo "- ghcr.io/${{ github.repository }}-auth:latest" >> $GITHUB_STEP_SUMMARY
          echo "- ghcr.io/${{ github.repository }}-tasks:latest" >> $GITHUB_STEP_SUMMARY
          echo "- ghcr.io/${{ github.repository }}-notifications:latest" >> $GITHUB_STEP_SUMMARY
          echo "- ghcr.io/${{ github.repository }}-gateway:latest" >> $GITHUB_STEP_SUMMARY